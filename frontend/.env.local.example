# Copy to .env.local for local dev. In production this is set as an
# Environment Variable in the Vercel project settings instead.

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SIMULATOR_API_KEY=
